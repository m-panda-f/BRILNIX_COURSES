specVersion: "v1beta"
serviceId: "courses"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "courses-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
